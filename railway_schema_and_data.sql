CREATE TABLE IF NOT EXISTS football_opportunities (id integer NOT NULL, timestamp bigint, odds real, edge_percentage real, confidence integer, stake real, payout real, settled_timestamp bigint, roi_percentage real, profit_loss real, quality_score real, daily_rank integer, model_prob real, calibrated_prob real, kelly_stake real, tier text, model_version text, publish_status text, match_date text, kickoff_time text, outcome text, actual_score text, match_id text, home_team text, away_team text, league text, market text, selection text, updated_at text, bet_category text, recommended_tier text, analysis text, recommended_date text, status text, result text);

CREATE TABLE IF NOT EXISTS sgp_predictions (id integer NOT NULL, timestamp bigint, parlay_probability real, fair_odds real, bookmaker_odds real, ev_percentage real, stake real, kelly_stake real, payout real, profit_loss real, settled_timestamp bigint, simulations integer, pricing_metadata text, correlation_method text, pricing_mode text, model_version text, status text, match_id text, home_team text, away_team text, league text, match_date text, kickoff_time text, legs text, parlay_description text, outcome text, result text);

